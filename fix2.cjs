const fs = require('fs'); const path = require('path'); const walk = dir => fs.readdirSync(dir).forEach(file => { const fullPath = path.join(dir, file); if (fs.statSync(fullPath).isDirectory()) { walk(fullPath); } else if (fullPath.endsWith('.tsx') || fullPath.endsWith('.ts')) { let content = fs.readFileSync(fullPath, 'utf8'); let newContent = content.split('target="_blank"').join('target="_blank" rel="noopener noreferrer"'); newContent = newContent.split('rel="noopener noreferrer" rel="noopener noreferrer"').join('rel="noopener noreferrer"'); if (content !== newContent) { fs.writeFileSync(fullPath, newContent); console.log('Updated ' + fullPath); } } }); walk('./src');
